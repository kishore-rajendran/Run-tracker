<br>
<br>
{{#each this.model.posts as |post|}}
<Post @name={{this.model.name}} @image={{post.image}} @title={{post.title}} @content={{post.content}} />
<br>
{{/each}}
<LinkTo id="myBtn" @route="create-post">+</LinkTo>